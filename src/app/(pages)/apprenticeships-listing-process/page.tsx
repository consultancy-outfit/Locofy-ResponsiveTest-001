import { ApprenticeshipsListingProcessImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const ApprenticeshipsListingProcessPage = () => {
  return (
    <CommonPage
      pageTitle={`Apprenticeships / Apprenticeships  Listing Process`}
      src={ApprenticeshipsListingProcessImage}
      backRoute="/"
    />
  );
};

export default ApprenticeshipsListingProcessPage;
