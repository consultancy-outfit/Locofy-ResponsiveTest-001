import { Section3SaleOrTransferImage } from "@/assets";
import { CommonPage } from "@/components";
import React from "react";

const Section3SaleOrTransferPage = () => {
	return (
		<CommonPage
			pageTitle={`Section 3 : Sale or Transfer`}
			src={Section3SaleOrTransferImage}
			backRoute="/adult-social-care-1"
		/>
	);
};

export default Section3SaleOrTransferPage;
